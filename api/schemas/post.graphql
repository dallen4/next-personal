# import User from 'user.graphql'

type Post {
    id: String!
    author: User!
    categories: [Category]!
    title: String!
    slug: String!
    description: String
    body: String!
}

type Category {
    id: String!
    value: String!
    label: String!
}
